# Dependencias núcleo (equivalente a [project.dependencies] de pyproject.toml).
# Fuente de verdad: pyproject.toml. Este archivo es para `pip install -r`.
# Extras (embeddings/rag/viz/notebook) se instalan aparte, ver README.md.
pymilvus>=2.5
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
python-dotenv>=1.0
tqdm>=4.66
