# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
dist/

# Entornos virtuales
.venv/
venv/
env/
.conda/

# uv
uv.lock  # descomentar para versionar el lock; por defecto lo ignoramos hasta fijar deps

# === Datos y modelos ===
# Se versiona data/raw/stratified_sample_2019_2026.csv (dataset fuente de la tesis).
# Todo lo derivado se ignora, incluidas las muestras de scripts/sampling_for_clustering/
# (regenerables desde la BD; destino documentado data/clustering_samples/, no data/raw/).
data/processed/*
!data/processed/.gitkeep
data/embeddings/
data/interim/
data/clustering_samples/
data/raw/stratified_by_seccion_all.csv
data/raw/stratified_by_seccion_thematic.csv
data/raw/stratified_grid_2019_2026.csv
models/
*.npy
*.npz
*.parquet
*.faiss

# === Docker / Milvus ===
docker/volumes/

# === Notebooks ===
.ipynb_checkpoints/

# === Secretos y entorno ===
.env
.env.local
*.key

# === IDE / SO ===
.vscode/
.idea/
.DS_Store
